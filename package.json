{
  "name": "shoplixo",
  "version": "1.0.0",
  "description": "Bangladesh's #1 Dropshipping Store — Shoplixo Vercel Backend",
  "private": true,
  "engines": {
    "node": ">=20.x"
  },
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod",
    "build": "echo 'No build step needed'",
    "lint": "eslint api/**/*.js --max-warnings 0",
    "test": "node --test api/**/*.test.js"
  },
  "dependencies": {
    "mongoose": "^8.9.0",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2",
    "nodemailer": "^6.9.13",
    "cors": "^2.8.5",
    "multer": "^1.4.5-lts.1",
    "cloudinary": "^2.2.0"
  },
  "devDependencies": {
    "vercel": "^33.5.0",
    "eslint": "^8.57.0"
  }
}
